<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.nordeck.wiki.reader"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".WikiReaderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.top.ActivityTopPages"/>

        <activity
            android:name=".ui.viewer.ActivityArticleViewer"/>

        <activity
            android:name=".ui.search.ActivitySearchPages"/>

        <activity
            android:name=".ui.wiki.ActivityWikis"/>

        <activity android:name=".ui.ActivitySplash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
